# security
api.security.token.secret=${JWT_SECRET:system-secret-key}

# postgres
spring.datasource.username=aga2ocom_usr
spring.datasource.password=89c2051

# datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/aga2ocom_raildb_dev_v1
spring.datasource.driver-class-name=org.postgresql.Driver

# jpa
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#spring.jpa.defer-datasource-initialization=true
#spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# server port
server.port=8081

#spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true 

# Disable feature detection by this undocumented parameter. Check the org.hibernate.engine.jdbc.internal.JdbcServiceImpl.configure method for more details.
#spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false

# Because detection is disabled you have to set correct dialect by hand.
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect



